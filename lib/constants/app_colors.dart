import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF990000);
  static const primaryDarkVariant = Color(0xFF330000);
  static const background = Color(0xFFFFEFD5);
  static const primaryGradient = [primary, primaryDarkVariant];
}